{
  "version": 2,
  "builds": [
    {
      "src": "src/server.js",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["**/*"],
        "installCommand": "npm install && npm install pg pg-hstore"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/server.js"
    }
  ],
  "crons": [
    {
      "path": "/healthcheck",
      "schedule": "0 10 * * *"
    }
  ]
}